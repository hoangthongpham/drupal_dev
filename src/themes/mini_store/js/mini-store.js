/**
 * @file
 * mini_store behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.miniStore = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
